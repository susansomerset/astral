import ListPage from "../components/ListPage"

export default function Responded() {
  return (
    <ListPage
      title="Responded"
      columns={[]}
      rows={[]}
      idField="astral_job_id"
      loading={false}
    />
  )
}
