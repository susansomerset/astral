import ListPage from "../components/ListPage"

export default function Applied() {
  return (
    <ListPage
      title="Applied"
      columns={[]}
      rows={[]}
      idField="astral_job_id"
      loading={false}
    />
  )
}
