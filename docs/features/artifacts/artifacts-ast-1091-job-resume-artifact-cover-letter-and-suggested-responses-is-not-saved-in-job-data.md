# AST-1091 — Job resume artifact, cover letter and suggested responses is not saved in job_data
**Component:** artifacts  
**Children:** AST-1099, AST-1100, AST-1116, AST-1117  
**Linear archived:** AST-1091 2026-08-07; AST-1099 2026-08-07; AST-1100 2026-08-07; AST-1116 2026-08-07; AST-1117 2026-08-07

## Ledger

| when (PT) | ticket | phase | sha | subject |
|---|---|---|---|---|
| 2026-07-30 21:41 | AST-1099 | docs | `118cdb047` | docs(AST-1099): plan — pin agent_data_id on job artifact slots |
| 2026-07-30 21:46 | AST-1099 | code | `417f5fc07` | code(AST-1099): pin agent_data_id on job artifact slots after hops |
| 2026-07-30 21:46 | AST-1099 | docs | `aad992e57` | docs(AST-1099): review stub — build tip 76928f73 |
| 2026-07-30 21:50 | AST-1099 | test | `449c61ccd` | test(AST-1099): pin agent_data_id on job artifact slots after hops |
| 2026-07-30 21:59 | AST-1099 | docs | `052b613a2` | docs(AST-1099): Radia review — DISCUSS (Joan stragglers) |
| 2026-07-30 21:59 | AST-1099 | resolve | `73b6b8be7` | resolve(AST-1099): — clean |
| 2026-07-30 21:59 | AST-1099 | merge-tests | `b3c1db724` | merge-tests(AST-1099): origin/tests 449c61ccd |
| 2026-07-30 21:59 | AST-1099 | test | `d010cab3c` | test(AST-1099): — no product fixes |
| 2026-07-30 22:44 | AST-1100 | docs | `4c44849fa` | docs(AST-1100): plan — resolve artifact bodies from pinned agent_data_id |
| 2026-07-30 22:53 | AST-1100 | docs | `1e047a2e2` | docs(AST-1100): review stub — build tip |
| 2026-07-30 22:53 | AST-1100 | code | `5ec54667e` | code(AST-1100): resolve pinned artifact bodies for UAT surfaces |
| 2026-07-30 22:56 | AST-1100 | merge-tests | `5859771fc` | merge-tests(AST-1100): origin/tests c1c77d531 |
| 2026-07-30 22:56 | AST-1100 | test | `c1c77d531` | test(AST-1100): resolve pinned artifact bodies for UAT surfaces |
| 2026-07-30 23:02 | AST-1100 | docs | `f3fb9270b` | docs(AST-1100): Radia review — DISCUSS (Joan stragglers) |
| 2026-07-30 23:04 | AST-1100 | resolve | `0b9706f26` | resolve(AST-1100): — clean |
| 2026-07-30 23:07 | AST-1100 | test | `5575ccb7a` | test(AST-1100): resolve pinned artifact bodies for UAT surfaces |
| 2026-07-30 23:07 | AST-1100 | merge-tests | `83cef96ca` | merge-tests(AST-1100): origin/tests c1c77d531 |
| 2026-07-30 23:13 | AST-1091/1099/1100 | merge | `dcfce070f` | merge origin/dev into ftr/ast-1091-job-artifact-agent-data-pins |
| 2026-07-30 23:14 | AST-1091 | prep-uat | `769d6c853` | prep-uat(ast-1091): rebuild merge ticket log |
| 2026-07-31 17:38 | AST-1116 | docs | `ac63308c5` | docs(AST-1116): plan — cover letter field defs for UAT preview |
| 2026-07-31 17:39 | AST-1116 | docs | `81e7edccc` | docs(AST-1116): review stub — build tip |
| 2026-07-31 17:39 | AST-1116 | code | `e550a2c80` | code(AST-1116): cover letter field defs + hydrate normalize |
| 2026-07-31 17:43 | AST-1116 | merge-tests | `0b5724a6b` | merge-tests(AST-1116): origin/tests 72ccddd58 |
| 2026-07-31 17:43 | AST-1116 | test | `72ccddd58` | test(AST-1116): cover letter field defs + hydrate normalize coverage |
| 2026-07-31 17:46 | AST-1116 | docs | `0acf29aaa` | docs(AST-1116): Radia review — clean |
| 2026-07-31 17:48 | AST-1116 | resolve | `593d6e009` | resolve(AST-1116): — clean |
| 2026-07-31 17:50 | AST-1117 | docs | `ecc5edf1e` | docs(AST-1117): plan — print HTML blobs via /candidate proxy |
| 2026-07-31 17:52 | AST-1117 | docs | `3bd55ec88` | docs(AST-1117): review stub — build tip |
| 2026-07-31 17:52 | AST-1117 | code | `c2788bfd8` | code(AST-1117): proxy /candidate + SPA catch-all guard |
| 2026-07-31 17:55 | AST-1117 | test | `7da983d32` | test(AST-1117): SPA candidate guard + Vite /candidate proxy coverage |
| 2026-07-31 17:55 | AST-1117 | merge-tests | `dcab534f0` | merge-tests(AST-1117): origin/tests 7da983d32 |
| 2026-07-31 17:58 | AST-1117 | docs | `56841f9d6` | docs(AST-1117): Radia review — clean |
| 2026-07-31 18:00 | AST-1117 | resolve | `048d966df` | resolve(AST-1117): — clean |
| 2026-07-31 18:01 | AST-1091 | prep-uat | `fd91dc01b` | prep-uat(ast-1091): rebuild merge ticket log |
| 2026-08-07 18:23 | AST-1099 | docs | `ebf041f80` | docs(AST-1099): archive Linear issue content |
| 2026-08-07 18:23 | AST-1100 | docs | `e165b06e8` | docs(AST-1100): archive Linear issue content |
| 2026-08-07 18:24 | AST-1116 | docs | `491edc969` | docs(AST-1116): archive Linear issue content |
| 2026-08-07 18:24 | AST-1117 | docs | `fd02f4736` | docs(AST-1117): archive Linear issue content |
| 2026-08-07 18:27 | AST-1091 | docs | `1262ad311` | docs(AST-1091): archive Linear issue content |

_Three rows are cross-ticket test-tree hygiene from unrelated tickets in other folders, matched only because their commit bodies name AST-1099: `resolve(AST-1105): — qa-handoff for AST-1099 test bleed`, `test(AST-1105): scrub orphan AST-1099/1100 test+bible bleed`, `test(AST-1102): refresh-ftr resolve test_agent.py keep AST-1099+AST-1083`. AST-1105 (`docs/features/contact/`) and AST-1102 (`docs/features/interface/`) are not AST-1091 family work — omitted from the table above._

## Epic — AST-1091
_Archived: 2026-08-07 · Linear URL: https://linear.app/astralcareermatch/issue/AST-1091/job-resume-artifact-cover-letter-and-suggested-responses-is-not-saved · Status at archive: Archive · Project: Astral Artifacts · Assignee: chuckles · Priority / estimate: Urgent / — · Blocked by / blocks / related: —_

### Purpose

The build-artifacts daisy chain already stores hop responses in `agent_data`, but nothing on the job points at the published resume / cover letter / suggested-answers hops — so JAR and candidate review have no artifact to open even when the chain succeeded. This epic pins each published hop by saving that hop's `agent_data_id` under `job_data.artifacts`, using the same core before/after-`do_task` persist style already used elsewhere — not a new TASK_CONFIG `persist_in` dialect and not a second copy of the response body on the job row.

### Functional scope

After a successful `finalize_job_resume` hop for a job, `job_data.artifacts.job_resume` holds that hop's RESPONSE `agent_data_id` (whether or not `run_next` continues).

After a successful `finalize_cover_letter` hop for a job, `job_data.artifacts.cover_letter` holds that hop's RESPONSE `agent_data_id` (whether or not `run_next` continues).

After a successful `propose_application_responses` hop for a job, `job_data.artifacts.proposed_answers` holds that hop's RESPONSE `agent_data_id`.

Core components that already know the job and the hop outcome (before/after `do_task`, same spirit as existing consult/candidate persist) perform the write — not a new generic TASK_CONFIG destination field, and not duplicating the parsed JSON into `job_data`.

Readers that need the artifact body load `agent_data` by the stored id (existing agent_data read paths). Surfaces that show these three artifacts for UAT resolve through those ids.

Failed hops do not write or clear a good prior id with an empty value. When `debug=True` on the touched persist path, log which key was written and which `agent_data_id` was recorded (or why skipped).

### Architectural definition

* **Patterns to reuse:** `pattern.batch.entity-agent-responses` (RESPONSE rows in `agent_data` remain the content store; `job_data.artifacts.*` holds a pointer, not a revived entity JSON `agent_responses` blob); `pattern.layers.import-discipline` (core decides when to pin; data layer saves job_data / reads agent_data); `pattern.config.config-block` (task keys and artifact slot names stay config/convention aligned; no new `persist_in` field).
* **New patterns proposed:** none. Explicit choice: do **not** introduce TASK_CONFIG `persist_in`.
* **Applicable statutes:** `astral.batch.entity-agent-responses-latest-only` (pin by `agent_data_id`; no entity-row mirrors); `astral.standards.in-scope-only` (only the three pointer slots + the write path + reader resolve); `astral.standards.dry-and-focused-functions` (extend existing post-`do_task` / tracker job_data merge patterns; no parallel persist framework); `astral.standards.debug-contract-gated` / `astral.standards.logging-via-utils`; `astral.standards.data-raises-caller-logs`; `astral.patterns.coat-check-never-store-empty` (do not store empty ids); `astral.layers.import-direction` (UI → core → data).

### Boundaries

Does not add TASK_CONFIG `persist_in` (or any new task-level destination dialect). Does not migrate `save_prefix` grade writes, candidate craft persists, or `analysis_upshot` one-offs onto a shared config field. Does not copy full hop JSON into `job_data.artifacts` for these three slots — pointer only. Does not redesign Manage Tasks `run_next` graphs or rename task keys. Does not change session cover letter / session resume paste. Does not reintroduce entity JSON `agent_responses` columns. Does not require changing unrelated JAR tabs or Materials Preview beyond resolving these three pointers for display/UAT. Does not alter graduation to CANDIDATE_REVIEW beyond what is required so pin writes stay consistent with a successful chain.

### Acceptance criteria

1. After a successful `finalize_job_resume` hop (chain may continue), `job_data.artifacts.job_resume` equals that hop's RESPONSE `agent_data_id` and that id loads the hop body from `agent_data`.
2. After a successful `finalize_cover_letter` hop (chain may continue), `job_data.artifacts.cover_letter` equals that hop's RESPONSE `agent_data_id` and that id loads the hop body from `agent_data`.
3. After a successful `propose_application_responses` hop, `job_data.artifacts.proposed_answers` equals that hop's RESPONSE `agent_data_id` and that id loads the hop body from `agent_data`.
4. A full successful daisy-chain that ran those three hops leaves all three pointer keys set; UAT surfaces that show Job Resume / Cover Letter / suggested answers resolve content via those ids without a manual PUT of the response body.
5. When `debug=True` on the touched persist path, each pin attempt logs key + `agent_data_id` (or skip reason); no new ungated `[DEBUG]` spam.
6. Failed or empty hops do not overwrite a good prior pointer with a blank value.

### Dependencies and blockers

none.

### Open questions

none.

### Proposed child tickets

**1[!]: Pin agent_data_id on job artifact slots after chain hops — Ada** — After successful `finalize_job_resume` / `finalize_cover_letter` / `propose_application_responses`, core (existing before/after-`do_task` persist style — not a new TASK_CONFIG field) writes `job_data.artifacts.job_resume` / `cover_letter` / `proposed_answers` as that hop's RESPONSE `agent_data_id`, including mid-chain when `run_next` continues. Debug found/recorded. Does not own JAR/UI resolve (#2).
**Citations:** `pattern.batch.entity-agent-responses`; `astral.batch.entity-agent-responses-latest-only`; `astral.patterns.coat-check-never-store-empty`; `astral.standards.debug-contract-gated`.

**2: Resolve artifact bodies from pinned agent_data_id for UAT surfaces — Katherine** — Job Resume / Cover Letter / suggested-answers surfaces used in UAT load body content via the pinned `agent_data_id` on `job_resume` / `cover_letter` / `proposed_answers` (existing agent_data read paths). After #1.
**Citations:** `pattern.batch.entity-agent-responses`; `astral.layers.import-direction`; `astral.standards.in-scope-only`.

_(UAT bugs AST-1116 and AST-1117 were filed later as children during the fix-uat wave.)_

### Original brief

The daisy chain is working great, but the responses are not saved to job_data.

#### Comments

##### susan — 2026-07-31T02:36:39.724Z
Is this an opportunity to use task_config to specify where to save the output, and if it is not specified, that's not an error? Where else would we make that specification.

##### chuckles — 2026-07-31T02:40:09.604Z
@susan Yes — TASK_CONFIG is the right home.

1. Optional persist target on the task (working name `job_artifact_key`) naming the `job_data.artifacts` key. Absent → skip persist, not an error. Same spirit as consult `save_prefix` for grade writes.
2. Not BUILD_CONFIG as a second task→artifact map (dual source of truth). `artifact_shapes` stay shape/writer contracts only.
3. Not `agent_task` DB rows — those own prompts / `run_next`, not product persist.

Definition updated to adopt that; OQ #1 is only the exact field name.

##### chuckles — 2026-07-31T03:58:32.334Z
@susan Adopted your split and path shape:

1. Two children — #1 TASK_CONFIG `persist_in` declarations only; #2 `agent.do_task` wiring.
2. Parse + persist orchestration: `agent.py`. Write: `database.py` after core shape/coat-check (data stays dumb; no validation in database).
3. Value is relative: `artifacts.resume_content` (not `job_data.…`) — entity from TASK_CONFIG `entity_type`.

OQs cleared.

##### susan — 2026-07-31T04:04:48.982Z
I think we need to go ahead and identify and resolve the technical debt this introduces. We have other dispatch task responses that are saved in job_data (grade_do, etc.) and candidate artifacts like craft_base_resume, which should use the persist_in pattern and decommission the explicit setting in the core components.

##### chuckles — 2026-07-31T04:06:42.022Z
@susan Expanded for the debt pass — not just the three missing artifacts.

Inventory in scope: graded `save_prefix` writes, candidate craft post-`do_task` persists, `analysis_upshot` one-offs. Coat-check/scrape writers stay out.

Two OQs in the Description:
1. Graded map shape (`grades`/`score`/`notes`/`rubric` → keys like `do_grades`)
2. Whether rubric-craft + company-search-terms join `craft_resume_base` in this epic

##### susan — 2026-07-31T04:26:41.168Z
Actually, I'm being wrong-headed about the whole thing. Just persist the agent_data_id on the job_data.artifacts.{job_resume|cover_letter|proposed_answers}, don't create additional tech debt with persist_in at all, just use the existing patterns for persisting the data from the core components that know what they're doing before and after calling do_task.

_(This reverses the entire TASK_CONFIG `persist_in` direction — including the expanded tech-debt migration of `grade_do` / `craft_resume_base` persists — adopted across the four comments above. The epic redefinition below is the result.)_

##### chuckles — 2026-07-31T04:27:48.051Z
@susan Got it — pivoted hard.

Dropped `persist_in` and the debt-migration scope. Definition is now: pin RESPONSE `agent_data_id` on `job_data.artifacts.job_resume` / `cover_letter` / `proposed_answers` via existing core before/after-`do_task` persist style; body stays in `agent_data`. Two children (pin + UAT resolve).

_(Cursor-chats `[thread-missing]` / `[thread-orphan]` store.db relocation notes and the `[refresh-ftr]` bible-conflict resolution comments are process narration — omitted; the merge itself is in the Ledger.)_

### Files changed (plan vs actual)

_No product commit trail on the parent — the two `prep-uat(ast-1091)` commits rebuild the merge ticket log; one `merge origin/dev` commit folds AST-1099/1100 up to `ftr`. Implementation landed via the four sub-issues below._

_Implementation detail may live in git history on `origin/dev`._

## Sub-issues

### AST-1099 — Pin agent_data_id on job artifact slots after chain hops
_Archived: 2026-08-07 · Linear URL: https://linear.app/astralcareermatch/issue/AST-1099/pin-agent-data-id-on-job-artifact-slots-after-chain-hops-job-resume · Status at archive: Archive · Project: Astral Artifacts · Assignee: ada · Priority / estimate: — · Blocked by / blocks / related: parent: AST-1091 (AC reference only)_

#### What this implements

After a successful `finalize_job_resume` / `finalize_cover_letter` / `propose_application_responses` hop, core writes that hop's RESPONSE `agent_data_id` into `job_data.artifacts.job_resume` / `cover_letter` / `proposed_answers` — including mid-chain when `run_next` continues. Bodies stay in `agent_data`; this ticket pins pointers only. JAR/UI resolve is AST-1100.

#### In scope / considered but excluded

In scope: `astral.config.config-source-of-truth`, `astral.standards.dry-and-focused-functions`, `astral.batch.entity-agent-responses-latest-only`, `astral.patterns.coat-check-never-store-empty`, `astral.standards.debug-contract-gated`. Considered but excluded: JAR/Materials readers resolving pinned ids (AST-1100); `JOBS_RECOMMENDED_ARTIFACT_TABS` remaps (AST-1100); TASK_CONFIG `persist_in` (parent forbids); `save_prefix` grade/craft/`analysis_upshot` migration (parent forbids).

#### Acceptance criteria

Parent AC1–3 (pin write for the three slots), AC5 (debug found/recorded), AC6 (no blank overwrite of a good prior pointer).

#### Files Changed (planned)

| File | Change | Layer |
|------|--------|-------|
| `src/utils/config.py` | Add task_key → artifact slot map; extend `JOB_BUILD_ARTIFACT_CLEAR_KEYS` with pin slots | utils |
| `src/core/tracker.py` | Add `pin_job_artifact_agent_data_id` (non-empty id only; Style D found/recorded when `debug=True`) | core |
| `src/core/agent.py` | After successful RESPONSE store for the three task keys, call pin regardless of `run_next`; stop terminal body-copy via `persist_job_artifact_from_parsed` for `finalize_job_resume` / `finalize_cover_letter` | core |

**Out of scope:** JAR / Materials Preview / API readers resolving pinned ids (AST-1100); `JOBS_RECOMMENDED_ARTIFACT_TABS` `artifact_key` remaps (AST-1100); TASK_CONFIG `persist_in`; `save_prefix` grade / craft / `analysis_upshot` persists; `tests/` / bible (Betty).

#### Stage 1: Config — pin map + cancel clear keys

**Done when:** `JOB_ARTIFACT_AGENT_DATA_PIN_BY_TASK` maps the three task keys to the three slot names; `JOB_BUILD_ARTIFACT_CLEAR_KEYS` includes the three pin slots so cancel does not leave stale pointers.

```python
# AST-1099: do_task pins RESPONSE agent_data_id under job_data.artifacts[<slot>] (pointer only).
JOB_ARTIFACT_AGENT_DATA_PIN_BY_TASK = {
    "finalize_job_resume": "job_resume",
    "finalize_cover_letter": "cover_letter",
    "propose_application_responses": "proposed_answers",
}
```
`JOB_BUILD_ARTIFACT_CLEAR_KEYS` becomes `("resume_content", "cover_letter", "application_responses", "job_resume", "proposed_answers")` — legacy body keys stay so cancel still wipes old blobs.

⚠️ **Decision:** Slot names are exactly `job_resume` / `cover_letter` / `proposed_answers` per parent AC (not `resume_content` / `application_responses`). `cover_letter` becomes a pointer string under this epic; AST-1100 updates readers.

#### Stage 2: Tracker — pin helper (never store empty)

**Done when:** `pin_job_artifact_agent_data_id` merges a non-empty string id into `job_data.artifacts[slot]`; blank/None/whitespace id skips the write and does not clear a prior value; `debug=True` Style D detail logs recorded key+id or skip reason; `debug=False` adds no new debug-contract lines.

`pin_job_artifact_agent_data_id(astral_job_id, artifact_key, agent_data_id, *, debug=False) -> bool`, next to `save_job_artifact_cover_letter` / `persist_job_artifact_from_parsed`: missing/blank `astral_job_id` → `False` (skip `missing_job_id`); missing/blank `artifact_key` → `False` (skip `missing_artifact_key`); coerce `agent_data_id` with `str(...).strip()`, empty → `False` without calling `save_job_data` (skip `empty_agent_data_id` — coat-check never-store-empty); on success `save_job_data(astral_job_id, {"artifacts": {artifact_key: agent_data_id}})` (deep merge). `debug=True`: `debug_detail` recorded line `artifact_pin key=<artifact_key> agent_data_id=<id> recorded` or skip line with `reason=<reason>`. Do **not** rewrite `persist_job_artifact_from_parsed` body-merge helpers here — Stage 3 stops calling them for the pin task keys from `do_task`.

⚠️ **Decision:** Pointer value is the bare RESPONSE `agent_data_id` string (same id returned by `_store_response_block`), not a nested `{agent_data_id: …}` object — matches parent AC "equals that hop's RESPONSE `agent_data_id`".

#### Stage 3: Agent — pin after successful RESPONSE store (mid-chain and terminal)

**Done when:** On a successful hop for each of the three task keys, after a RESPONSE row is stored and its id is known, `job_data.artifacts[<slot>]` equals that id whether or not `run_next` continues; failed/empty id paths leave a prior good pointer untouched; terminal `persist_job_artifact_from_parsed` no longer body-copies for `finalize_job_resume` / `finalize_cover_letter`.

1. Import `JOB_ARTIFACT_AGENT_DATA_PIN_BY_TASK` from `src.utils.config`. Locate the success RESPONSE store block (`if _should_store and raw_text:` → `resp_id = _store_response_block(...)`). Immediately after a successful `resp_id` assignment: `slot = JOB_ARTIFACT_AGENT_DATA_PIN_BY_TASK.get(task_key)`; if `slot` set and `index` truthy and `resp_id` non-empty, lazy-import `pin_job_artifact_agent_data_id` from `src.core.tracker` (cycle-break style already used for `persist_job_artifact_from_parsed`) and call it. If `slot` set but pin cannot run (no `index`, no `resp_id`, store exception), log the skip reason when `debug=True` — do **not** `save_job_data` with a blank id.
2. Placement must be **before** the `run_next` recurse branch so mid-chain pins fire — repo `agent_task.json` has `finalize_job_resume.run_next = draft_cover_letter` and `finalize_cover_letter.run_next = propose_application_responses`, so those hops never reached the old terminal-only body persist.
3. In the terminal block that calls `persist_job_artifact_from_parsed` when `not effective_next`, **remove** the `finalize_job_resume` / `finalize_cover_letter` body-copy path — pointer pin from step 1 is the sole write for those hops. Leave `persist_job_artifact_from_parsed` and the PUT helpers intact for manual API / other callers.
4. Do not pin on failure RESPONSE stores. Do not add TASK_CONFIG fields. Do not edit UI/API files.

⚠️ **Decision:** Stop `do_task` body-copy for the two finalize hops so `artifacts.cover_letter` is not overwritten by a dict after a pointer string pin (same key). `resume_content` body writes from that terminal path are also stopped for `finalize_job_resume`; the authoritative resume pointer is `job_resume`. Manual `PUT …/artifacts/resume_content` remains for editors until AST-1100 remaps surfaces. _(This "pointer-only, no sibling blob" decision was walked back for `resume_content` specifically by the later AST-1428 gap — see that ticket's own family archive.)_

**Self-Assessment:** Single-Component — config map + tracker pin helper + `do_task` post-RESPONSE persist only; no UI, no schema, no TASK_CONFIG dialect. Conf high — reuses existing `_store_response_block` id + `save_job_data` deep-merge artifacts pattern; mid-chain gap is explicit in `agent_task.json` `run_next` and the terminal-only guard. Risk Medium — `artifacts.cover_letter` type changes from object to `agent_data_id` string for chain-written jobs — JAR/readers stay broken until AST-1100 (intentional, blockedBy this ticket); a bad pin (wrong id / skipped mid-chain) leaves UAT surfaces empty.

##### Comments

###### ada — 2026-07-31T04:41:42.179Z
Plan @ `2cc10e72`. Approach: config map → tracker pin helper (never store empty) → `do_task` pins after successful RESPONSE store **before** `run_next` (fixes mid-chain gap); stops terminal body-copy for the two finalize hops so `cover_letter` is not overwritten by a dict after the pointer string. Single-Component; Conf high; Risk Medium.

###### joan — 2026-07-31T04:44:01.910Z (plan-rubric.v1)
**Overall: APPROVED.** Stage 1 config map + clear keys → AC1–3; Stage 2 tracker pin helper (coat-check, debug) → AC5/AC6; Stage 3 `do_task` pin before `run_next` + stop finalize body-copy → mid-chain AC + pointer-only Boundary. All in-scope statutes **conforms**. No `fix-now`.

###### betty — 2026-07-31T04:51:02.394Z
QA manifest AST-1099: pin map / helper / mid-chain pins across `test_agent.py`, `test_tracker.py`, `test_config.py`. Publish `@ b3c1db72` (`merge-tests(AST-1099): origin/tests 449c61cc`); a later `test(AST-1099): — no product fixes` commit followed a bible/test-tree conflict resolve during `refresh-ftr` (see epic ledger).

###### radia — 2026-07-31T04:55:52.910Z (code-rubric.v1)
**Overall: DISCUSS** (procedural straggler only). Config map + clear-key extension match Stage 1 exactly; `pin_job_artifact_agent_data_id` coat-checks empty/blank ids; Style D gated; `do_task` pins after successful RESPONSE store and **before** `run_next`; terminal body-copy removed for finalize hops; lazy tracker import keeps the documented agent↔tracker cycle break; Betty one-SHA `merge-tests` + component coverage. All in-scope statutes **conforms**. **discuss (C4 straggler):** Joan-excluded `astral.docs.features-single-file-per-ticket`, `astral.git.engineer-test-tree-ban`, `astral.debug.spikes-under-debug-dir` — in-scope on the ticket diff, each **conforms**. No `fix-now`. _Note: formal `origin/dev...` three-dot was epic-ancestry polluted; review used the AST-1099 commit change set directly (`2cc10e72` / `76928f73` / `449c61cc` / `fe09931b`)._

#### Resolution (2026-07-31)

Clean — no product fix-now. discuss (C4 straggler): Radia confirmed the three Joan-excluded statutes **conform** on the tip. No product edit. AST-1100 remains the reader/remap sibling — the pointer-string gap on UAT surfaces until then is intentional.

#### Files changed (plan vs actual)

| | file | planned | actual |
|---|---|---|---|
| ✓ | `src/utils/config.py` | Task_key → artifact slot map; extend `JOB_BUILD_ARTIFACT_CLEAR_KEYS` with pin slots | `417f5fc07` |
| ✓ | `src/core/tracker.py` | `pin_job_artifact_agent_data_id` (non-empty id only; Style D) | `417f5fc07` |
| ✓ | `src/core/agent.py` | Pin after successful RESPONSE store for the three task keys, before `run_next`; stop terminal body-copy for the two finalize hops | `417f5fc07` |
| | _tests_ | pin map / helper / mid-chain pins | `449c61ccd` — `test_agent.py` / `test_tracker.py` / `test_config.py` + test-bible (Betty) |

### AST-1100 — Resolve artifact bodies from pinned agent_data_id for UAT surfaces
_Archived: 2026-08-07 · Linear URL: https://linear.app/astralcareermatch/issue/AST-1100/resolve-artifact-bodies-from-pinned-agent-data-id-for-uat-surfaces-job · Status at archive: Archive · Project: Astral Artifacts · Assignee: katherine · Priority / estimate: — · Blocked by / blocks / related: parent: AST-1091 (AC reference only)_

#### What this implements

After AST-1099 pins RESPONSE `agent_data_id` under `job_data.artifacts.job_resume` / `cover_letter` / `proposed_answers`, UAT surfaces (JAR Artifacts tabs, print/Materials Preview, job HTML builders) must load the hop body via existing `agent_data` read paths — no manual PUT of the response JSON onto the job row.

#### In scope / considered but excluded

Same statute set as AST-1099 for the read side; excluded: pin write itself (AST-1099, already on `ftr`), TASK_CONFIG `persist_in`, unrelated JAR chrome/tab redesign, session cover/resume paste.

#### Acceptance criteria

Parent AC4 (UAT surfaces resolve via the pinned ids without a manual PUT); AC5 (debug on the resolve path when `debug=True`); AC6 (hydrate/resolve skip blank/missing without writing empties).

#### Files Changed (planned)

| File | Change | Layer |
|------|--------|-------|
| `src/utils/config.py` | Remap `JOBS_RECOMMENDED_ARTIFACT_TABS` `artifact_key` values to the three pin slots | utils |
| `src/core/tracker.py` | Add pin→body resolve + display hydrate helpers (read-only overlay; coat-check empty) | core |
| `src/core/builder.py` | Prefer resolved pin bodies for job resume / cover letter HTML when legacy body dicts are absent | core |
| `src/ui/api/api_jobs.py` | Hydrate artifacts on job GET; accept PUT for remapped keys (map to existing body saves) | ui |
| `src/ui/frontend/src/lib/recommendedJobReport.tsx` | Visibility helpers use remapped pin keys (after hydrate, values are bodies) | ui |

**Out of scope:** writing / mid-chain pin of `agent_data_id` (AST-1099, already on `ftr`); TASK_CONFIG `persist_in` (parent forbids); unrelated JAR chrome / tab redesign; session cover letter / session resume paste; `tests/` / bible product coverage (Betty).

#### Stage 1: Config — remap JAR artifact tab keys

**Done when:** `JOBS_RECOMMENDED_ARTIFACT_TABS` points Job Resume / Cover Letter / Application Questions at `job_resume` / `cover_letter` / `proposed_answers`; `shapes_key` for cover stays `"cover_letter"`; Job Resume keeps `use_resume_structure: True`.

```python
JOBS_RECOMMENDED_ARTIFACT_TABS = [
    {"tab_id": "artifact_resume", "nav_label": "Job Resume", "artifact_key": "job_resume", "shapes_key": None, "use_resume_structure": True},
    {"tab_id": "artifact_cover", "nav_label": "Cover Letter", "artifact_key": "cover_letter", "shapes_key": "cover_letter", "use_resume_structure": False},
    {"tab_id": "artifact_application", "nav_label": "Application Questions", "artifact_key": "proposed_answers", "shapes_key": None, "use_resume_structure": False},
]
```

⚠️ **Decision:** Remap to the AST-1099 pin slot names (not keep `resume_content` / `application_responses` as tab keys). Legacy body keys remain on the job for older rows and for PUT body-storage aliases in Stage 3; display hydrate (Stage 2) bridges pin → body for the remapped keys.

#### Stage 2: Tracker — resolve pin id → body + display hydrate

**Done when:** Given a non-empty pin string, resolve returns the parsed RESPONSE body from `agent_data` (same parse spirit as mid-chain hydration); blank/missing id or missing row returns `None` without writing; hydrate returns a shallow-copied `artifacts` dict where each pin-slot string is replaced by the resolved body when resolve succeeds.

`resolve_job_artifact_agent_data_body(agent_data_id, *, debug=False) -> Any`: coat-check blank/None/whitespace → `None` (skip `empty_agent_data_id`); load the row via the same data API core already uses (`get_agent_data`); missing row → `None` (`missing_agent_data_row`); empty `block_data`/`content` → `None`; parse with the same rules as `src.core.agent._parsed_response_from_stored_response_text` via a lazy import (cycle-safe); `debug=True` → Style D `artifact_resolve agent_data_id=<id> recorded` (or skip).

`hydrate_job_artifacts_for_display(artifacts, *, debug=False) -> Dict[str, Any]`: non-dict input → `{}`; shallow-copy; for each pin key (`job_resume`, `cover_letter`, `proposed_answers`): non-empty `str` value → resolve, set `out[key]` only if `body is not None`; already-a-dict/list legacy body → leave as-is. Does **not** call `save_job_data` — display overlay only; stored pins stay strings on disk.

⚠️ **Decision:** Hydrate is response-overlay only. Editing via PUT may replace a pin string with a body dict on that key (Stage 3); initial UAT after a successful chain must work with zero PUTs.

#### Stage 3: API — hydrate on job GET; PUT aliases for remapped keys

**Done when:** `GET /api/jobs/<astral_job_id>` returns `job_data.artifacts` after `hydrate_job_artifacts_for_display`; PUT endpoints exist (or alias) so ArtifactEditor can save under remapped keys without 404.

1. `GET /api/jobs/<id>` handler: after loading the job row and before `jsonify`, replace `artifacts` with `hydrate_job_artifacts_for_display(...)` — do not persist the hydrated copy.
2. Keep existing `resume_content` / `cover_letter` / `application_responses` PUT routes. Generalize routes so `job_resume` and `proposed_answers` accept the same body shapes. **Required choice** (after weighing alternatives): `PUT job_resume` validates the dict body and `save_job_data(..., {"artifacts": {"job_resume": body}})` — the body dict replaces the pin string (acceptable after a human edit) — same key the tab reads on hydrated GET. Mirror for `proposed_answers`. `PUT cover_letter` already exists and is left as-is (dict overwrites pin string after edit).
3. Do not add new JAR routes beyond these aliases / hydrate.

#### Stage 4: Builder — HTML preview resolves pins

**Done when:** `build_resume(job_id)` / cover-letter HTML path can render from a pin when `resume_content` / cover body dict is missing.

`_resolve_resume_sections`: after checking `artifacts.resume_content`, if missing/empty and `artifacts.job_resume` is a non-empty string pin, resolve it (lazy-import from tracker); use the resolved dict if non-empty, else fall through to `base_resume`. `_resolve_cover_letter`: if `artifacts.cover_letter` is a non-empty string pin, resolve to body; if dict with fields, normalize via existing `_cover_letter_fields_for_read` / `normalize_cover_letter_artifact`; if resolve fails, keep the existing sample_cover fallback.

#### Stage 5: Frontend visibility helpers

**Done when:** Print / Materials Preview / `anyReportArtifactContent` gate on the remapped keys; hydrated GET bodies (dicts) still count as content; `artifactHasContent` also treats a non-empty string as content (pin present even if hydrate skipped).

`artifactHasContent`: after the object/array branches, a non-empty string also counts. `printResumeVisible` → `artifactHasContent(artifacts, "job_resume")` (fallback: also true if legacy `resume_content` has content). `printCoverVisible` → keep `"cover_letter"` (pin or body). `materialsPreviewVisible` → same resume/cover checks. Do **not** redesign `JobAnalysisReportModal` section layout — it already iterates `report_artifact_tabs` from the manifest.

**Self-Assessment:** Single-Component — config tab remap + tracker resolve/hydrate + builder pin read + jobs API GET/PUT aliases + small FE visibility helpers for the three pin slots. Conf high — reuses `get_agent_data` / `_parsed_response_from_stored_response_text` and existing JAR tab/ArtifactEditor wiring. Risk Medium — wrong parse of RESPONSE text leaves tabs empty after a green chain; PUT overwrite of `cover_letter` pin with an edited dict is intentional after human edit.

##### Comments

###### katherine — 2026-07-31T05:44:33.876Z
Plan @ `e45d2015`. Approach: remap tabs to pin slots → tracker resolve + hydrate (read-only overlay) → hydrate on job GET; builder prefers resolved pins when legacy body dicts missing; FE visibility helpers use remapped keys. Single-Component; Conf high; Risk Medium.

###### joan — 2026-07-31T05:47:09.148Z (plan-rubric.v1)
**Overall: APPROVED.** Stage 1 tab remap → config alignment; Stage 2 resolve + hydrate → load `agent_data` by stored id, coat-check empty; Stage 3 GET hydrate + PUT aliases → UAT works with zero PUTs; Stage 4 builder pin read → Materials/print HTML; Stage 5 FE visibility → Print/Materials gates. All in-scope statutes **conforms**. **discuss (non-blocking):** Stage 3 PUT step wanders through alternate options before the binding **Use:** — final choice is clear; tighten prose if revisited.

###### betty — 2026-07-31T05:59:15.766Z
QA manifest AST-1100: resolve/hydrate/builder/API/FE coverage across `test_builder.py`, `test_tracker.py`, `test_recommendedJobReport.test.tsx`, `test_api_jobs.py`, `test_config.py`; `stateUiManifestFixture.ts` aligned to remapped `artifact_key`s. Publish `@ 5859771f` (`merge-tests(AST-1100): origin/tests c1c77d53`).

###### radia — 2026-07-31T06:03:12.611Z (code-rubric.v1)
**Overall: DISCUSS** (procedural straggler only). Tab remap to AST-1099 pin slots; GET hydrate via core helper; PUT aliases under remapped keys with `@require_auth`; `resolve_job_artifact_agent_data_body` coat-checks blank/missing, Style D gated, lazy agent-parse import documented; builder prefers legacy body → pin resolve → fallback; FE visibility accepts pin strings + hydrated bodies; UI imports core only. All in-scope statutes **conforms**. **discuss (C4 straggler):** Joan-excluded `astral.docs.features-single-file-per-ticket` and `astral.debug.spikes-under-debug-dir` — in-scope on the ticket diff, both **conform**. No `fix-now`.

#### Resolution (2026-07-31)

Clean — no product fix-now. discuss (C4 straggler): acknowledged, both **conform**, no product change. Pointer overwrite on human PUT remains intentional (plan Medium risk).

#### Files changed (plan vs actual)

| | file | planned | actual |
|---|---|---|---|
| ✓ | `src/utils/config.py` | Remap `JOBS_RECOMMENDED_ARTIFACT_TABS` `artifact_key` values to the three pin slots | `5ec54667e` |
| ✓ | `src/core/tracker.py` | Pin→body resolve + display hydrate helpers (read-only overlay; coat-check empty) | `5ec54667e` |
| ✓ | `src/core/builder.py` | Prefer resolved pin bodies for job resume / cover letter HTML when legacy body dicts absent | `5ec54667e` |
| ✓ | `src/ui/api/api_jobs.py` | Hydrate artifacts on job GET; PUT aliases for remapped keys | `5ec54667e` |
| ✓ | `src/ui/frontend/src/lib/recommendedJobReport.tsx` | Visibility helpers use remapped pin keys | `5ec54667e` |
| | _tests_ | resolve/hydrate/builder/API/FE coverage + fixture align | `c1c77d531` — `test_builder.py` / `test_tracker.py` / `test_recommendedJobReport.test.tsx` / `test_api_jobs.py` / `test_config.py` + test-bible (Betty) |

### AST-1116 — UAT: Cover Letter preview fails field definitions for cover_letter
_Archived: 2026-08-07 · Linear URL: https://linear.app/astralcareermatch/issue/AST-1116/uat-cover-letter-preview-fails-field-definitions-for-cover-letter · Status at archive: Archive · Project: Astral Artifacts · Assignee: katherine · Priority / estimate: — · Blocked by / blocks / related: parent: AST-1091 (AC reference only) · UAT bug filed by Chuckles from fix-uat_

#### What failed

Previewing the Cover Letter artifact on the recommended/JAR UAT surface shows `Failed to load field definitions for "cover_letter".`

#### Expected

Cover Letter preview loads field tabs and body content resolved from the pinned `job_data.artifacts.cover_letter` `agent_data_id` (via existing agent_data read paths).

#### Repro

1. Open a recommended job that has completed the cover-letter hop (pin present on `job_data.artifacts.cover_letter`).
2. Open the Cover Letter artifact / preview tab.
3. Observe the red error: Failed to load field definitions for "cover_letter".

#### Diagnosis / Root cause

**Hypothesis:** Cover Letter tab sets `shapesKey` to `cover_letter`, but `DATA_SHAPES.candidates.detail` has no `cover_letter` field-def list — ArtifactEditor treats empty defs as hard failure before pin-resolved body can render. **Wrong fix to avoid:** swallow `shapeError` and show a blank editor; put the full cover JSON back on `job_data.artifacts`; invent unrelated `DATA_SHAPES` keys without aligning `JOBS_RECOMMENDED_ARTIFACT_TABS.shapes_key` / ArtifactEditor contract; catch-all hide the error. **Related:** AST-1100 (resolve pin → body); AST-1099 (pin write) — pin-on-job contract must still hold.

#### Files Changed (planned)

| File | Change | Layer |
|------|--------|-------|
| `src/utils/config.py` | Add `DATA_SHAPES["candidates"]["detail"]["cover_letter"]` field-def list (Subject / Letter / signature) | utils |
| `src/core/tracker.py` | In `hydrate_job_artifacts_for_display`, when resolved/left `cover_letter` value is a dict, replace with `normalize_cover_letter_artifact(...)` (display overlay only) | core |

**Out of scope:** pin write / `JOB_ARTIFACT_AGENT_DATA_PIN_BY_TASK` (AST-1099); Print HTML / Materials print routes (AST-1117); TASK_CONFIG `persist_in`; unrelated JAR tabs / Job Resume `use_resume_structure`; `tests/` / bible (Betty).

#### Stage 1: Config — DATA_SHAPES cover_letter field defs

**Done when:** `GET /api/shapes/candidates` JSON includes non-empty `detail.cover_letter` with three fields keyed `Subject`, `Letter`, `signature`; `JOBS_RECOMMENDED_ARTIFACT_TABS` cover row still has `shapes_key: "cover_letter"`.

```python
# AST-1116: ArtifactEditor fixed tabs for JAR Cover Letter (shapes_key=cover_letter).
# Keys match BUILD_CONFIG["artifact_shapes"]["cover_letter"] + normalize_cover_letter_artifact.
"cover_letter": [
    {"key": "Subject", "label": "Subject", "type": "str"},
    {"key": "Letter", "label": "Letter", "type": "str"},
    {"key": "signature", "label": "Signature", "type": "str"},
],
```
Do not change `JOBS_RECOMMENDED_ARTIFACT_TABS`. Do not add other `DATA_SHAPES` keys or invent a parallel shapes API.

⚠️ **Decision:** Field keys are `Subject` / `Letter` / `signature` (canonical job cover spine), not hop RESPONSE `re_line` / `body`. Hop aliases are mapped in Stage 2 via existing `normalize_cover_letter_artifact` so fixed tabs fill after pin hydrate and after human PUT (which already normalizes on save).

#### Stage 2: Tracker — normalize cover body on display hydrate

**Done when:** After `hydrate_job_artifacts_for_display`, a pin string on `cover_letter` that resolves to `{re_line, body, signature}` (or Subject/Letter) becomes `{Subject, Letter, signature}` in the returned overlay dict; stored pins on disk are unchanged.

```python
# After pin resolve loop (and also if cover_letter was already a body dict):
cover = out.get("cover_letter")
if isinstance(cover, dict):
    out["cover_letter"] = normalize_cover_letter_artifact(cover)
```
Placed **after** the pin-key loop so both freshly resolved pin bodies and legacy body dicts get Subject/Letter keys. Does not call `save_job_data` / `save_job_artifact_cover_letter` from hydrate. Does not change `resolve_job_artifact_agent_data_body` itself.

**Self-Assessment:** Single-Component — `DATA_SHAPES` cover field defs in config + one normalize line in tracker display hydrate for the Cover Letter UAT tab. Conf high — failure mode is proven (`shapes.detail.cover_letter` empty → `shapeError`); fix reuses `normalize_cover_letter_artifact` and the existing `shapes_key` / `/api/shapes/candidates` contract. Risk Medium — wrong field keys leave tabs blank after the error clears; hydrate normalize bugs could mask raw RESPONSE shape for other GET-overlay consumers.

##### Comments

###### katherine — 2026-08-01T00:38:23.652Z
Plan @ `ac63308c`. Approach: add `DATA_SHAPES.candidates.detail.cover_letter` so ArtifactEditor stops hard-failing; normalize pin-resolved cover bodies in display hydrate via `normalize_cover_letter_artifact` so hop `re_line`/`body` fill those tabs. Single-Component; Conf high; Risk Medium.

###### radia — 2026-08-01T00:47:11.186Z (code-rubric.v1)
**Overall: CLEAN.** `DATA_SHAPES.candidates.detail.cover_letter` Subject/Letter/signature matches ArtifactEditor `shapes_key` and `normalize_cover_letter_artifact`; display hydrate normalizes the cover dict after pin resolve (overlay only, no save; pin id contract untouched); tab still `shapes_key: cover_letter`; wrong-fix paths not taken; Betty one-SHA `merge-tests`, engineer stayed off tests/bible. No fix-now / discuss.

###### betty — 2026-08-01T00:44:16.080Z
QA manifest AST-1116: field defs + hydrate normalize coverage in `test_tracker.py` / `test_config.py`. Publish `@ 0b5724a6` (`merge-tests(AST-1116): origin/tests 72ccddd5`).

#### Resolution (2026-08-01)

Clean. fix-now: none — Radia overall CLEAN. discuss / advisory: none.

#### Files changed (plan vs actual)

| | file | planned | actual |
|---|---|---|---|
| ✓ | `src/utils/config.py` | Add `DATA_SHAPES["candidates"]["detail"]["cover_letter"]` field-def list | `e550a2c80` |
| ✓ | `src/core/tracker.py` | Normalize `cover_letter` dict values via `normalize_cover_letter_artifact` on display hydrate | `e550a2c80` |
| | _tests_ | field defs + hydrate normalize coverage | `72ccddd58` — `test_tracker.py` / `test_config.py` / `test_api_system.py` + test-bible (Betty) |

### AST-1117 — UAT: Print Resume and Print Cover Letter open recommended page not HTML
_Archived: 2026-08-07 · Linear URL: https://linear.app/astralcareermatch/issue/AST-1117/uat-print-resume-and-print-cover-letter-open-recommended-page-not-html · Status at archive: Archive · Project: Astral Artifacts · Assignee: katherine · Priority / estimate: — · Blocked by / blocks / related: parent: AST-1091 · UAT bug filed by Chuckles from fix-uat_

#### What failed

Both **Print Resume** and **Print Cover Letter** open new browser tabs that land on the recommended page, not printable HTML blobs for the job resume / cover letter.

#### Expected

Print Resume / Print Cover Letter open tabs that render the HTML document built from the pinned artifact bodies (`job_data.artifacts.job_resume` / `cover_letter` → `agent_data`), suitable for print/PDF.

#### Repro

1. Open a recommended job whose daisy-chain left resume and cover pins set.
2. Click **Print Resume** — new tab shows the recommended page, not resume HTML.
3. Click **Print Cover Letter** — same: recommended page, not cover HTML.

#### Diagnosis / Root cause

**Hypothesis:** Vite proxies only `/api` today — Print `window.open('/candidate/…')` loads the SPA, and `*` redirects to `/jobs/recommended`. Flask already has `resume_html_bp` + AST-1100 pin resolve in the builder. **Wrong fix to avoid:** hide Print buttons when pins-only; open a blank tab; re-store full HTML/JSON on `job_data`; SPA-navigate inside the modal instead of serving HTML; swallow builder errors; relocate the HTML routes under `/api/…` (keep the stable AST-605 paths).

#### Files Changed (planned)

| File | Change | Layer |
|------|--------|-------|
| `src/ui/frontend/vite.config.ts` | Proxy `/candidate` → Flask `:5001` (same target as `/api`) | ui |
| `src/ui/server.py` | In SPA catch-all `serve_react`, do **not** serve `index.html` for paths under `candidate/` — return 404 JSON if the HTML blueprint did not handle the request | ui |

**Out of scope:** pin write / pin keys (AST-1099); Cover Letter ArtifactEditor field-defs (AST-1116); builder pin→body resolve (AST-1100, leave unless Stage 3 proves otherwise); session cover/resume paste; unrelated JAR / recommended chrome; `tests/` / bible (Betty).

#### Stage 1: Vite — proxy `/candidate` to Flask

**Done when:** `vite.config.ts` proxies `/candidate` to `http://localhost:5001`; local Vite UAT `window.open('/candidate/resume/…')` / cover hits Flask `resume_html_bp` instead of the SPA; no product URL changes required in the modal.

```ts
    proxy: {
      '/api': 'http://localhost:5001',
      '/candidate': 'http://localhost:5001',
    },
```
Do not change Print `window.open` paths in `JobAnalysisReportModal.tsx` / `MaterialsPreviewModal.tsx`. Do not add React Router routes for those paths.

⚠️ **Decision:** Fix the miss by proxying the existing AST-605 HTML routes rather than relocating them under `/api/…`. Keeps print/PDF URLs stable; Vite was the hole (`/api` only).

#### Stage 2: Flask — never SPA-fallback `/candidate/*`

**Done when:** `serve_react` returns 404 JSON for any `path` that starts with `candidate/` instead of `index.html`; blueprint routes under `resume_html_bp` continue to return `text/html` when matched.

```python
def serve_react(path):
    """Catch-all: React static assets. Never steal /candidate/* HTML routes."""
    if path == "candidate" or path.startswith("candidate/"):
        return jsonify({"error": "Not found"}), 404
    if (_DIST / path).is_file():
        return send_from_directory(_DIST, path)
    return send_from_directory(_DIST, "index.html")
```
Import `jsonify` from `flask` if not already imported. Do not move/rename `resume_html_bp` routes or remove `@require_auth` from them.

#### Stage 3: Verify pin-only HTML (no code unless broken)

**Done when:** With a job that has only pin strings on `job_resume` / `cover_letter` (no legacy body dicts), Flask `GET /candidate/resume/<job_id>` and `GET /candidate/cover/<job_id>` return `text/html` bodies that include resolved hop content. If both succeed via the existing AST-1100 builder resolve, **commit nothing in this stage**. If either fails for pin-only while the pin id is valid, **stop** and comment on AST-1117 with the exact exception — do not invent a second resolve path without that evidence. (As built: both succeeded — no Stage 3 code.)

**Self-Assessment:** Single-Component — Vite proxy + Flask SPA catch-all guard for `/candidate/*` HTML print routes. Conf high — symptom matches React `*` → `/jobs/recommended`; Vite proxy today is `/api` only; Flask already has `resume_html_bp` + AST-1100 pin resolve. Risk Medium — proxy misconfig could still 404 print tabs locally; an overly broad catch-all exclusion could hide a real missing-route bug as JSON 404 (preferable to the wrong recommended shell).

##### Comments

###### katherine — 2026-08-01T00:51:04.428Z
Plan @ `ecc5edf1`. Approach: Vite proxies only `/api` today — proxy `/candidate` → Flask; harden Flask `serve_react` so `candidate/*` never gets `index.html`. Builder pin resolve (AST-1100) stays; Stage 3 verifies only. Single-Component; Conf high; Risk Medium.

###### betty — 2026-08-01T00:55:35.448Z
QA manifest AST-1117: `TestAst1117CandidateSpaGuard` (catch-all 404 JSON for `candidate/*`, SPA fallback still for other paths), `TestAst1117ViteCandidateProxy` (`vite.config.ts` proxies `/candidate` → Flask), plus existing bible-backed `TestResumeHtmlRoutes` / `TestAst581CoverRoute` regressions. Publish `@ dcab534f` (`merge-tests(AST-1117): origin/tests 7da983d3`).

###### radia — 2026-08-01T00:59:35.481Z (code-rubric.v1)
**Overall: CLEAN.** Vite proxies `/candidate` to Flask `:5001` alongside `/api`; Flask SPA catch-all returns 404 JSON for `candidate/*` instead of `index.html`; `resume_html_bp` + `@require_auth` left intact; Stage 3 needed no builder code (AST-1100 pin resolve already present); engineer stayed off tests/bible. No fix-now / discuss. _Note: `linear-radia` MCP was unavailable at handoff — the Linear comment/Review-Posted step ran on a later pass; `docs()` was pushed from this run._

#### Resolution (2026-08-01)

Clean. fix-now: none — Radia overall CLEAN. discuss / advisory: none.

#### Files changed (plan vs actual)

| | file | planned | actual |
|---|---|---|---|
| ✓ | `src/ui/frontend/vite.config.ts` | Proxy `/candidate` → Flask `:5001` | `c2788bfd8` |
| ✓ | `src/ui/server.py` | SPA catch-all never serves `index.html` for `candidate/*` — 404 JSON instead | `c2788bfd8` |
| | _tests_ | candidate SPA guard + Vite proxy coverage | `7da983d32` — `test_server.py` + test-bible (Betty) |
