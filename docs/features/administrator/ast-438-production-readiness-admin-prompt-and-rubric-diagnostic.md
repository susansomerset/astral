# AST-438 — Production readiness: admin prompt and rubric diagnostic

**Linear:** [AST-438 — Production readiness: admin prompt and rubric diagnostic](https://linear.app/astralcareermatch/issue/AST-438/production-readiness-admin-prompt-and-rubric-diagnostic)  
**Parent:** [AST-434 — Production readiness: job-list rubrics UI + prompt/rubric diagnostic](https://linear.app/astralcareermatch/issue/AST-434/production-readiness-job-list-rubrics-ui-promptrubric-diagnostic)  
**Feature ref:** `sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic` (origin only)

## Summary

Read-only diagnostic for **Astral Administrator**: compare **local** vs **Railway production** `agent` and `agent_task` prompt content for consult + craft rubric tasks, then validate **Susan-designated** candidate rubric artifacts against `TASK_CONFIG` / save-time rules (`normalize_rubric_artifacts_on_save`, trailing grade tables, `importance`). Output is **reviewable markdown** under `docs/features/administrator/` plus raw captures under `debug/spikes/AST-438/` (gitignored). **No** prod writes, prompt sync, rubric regeneration, or changes to `do_task` / scoring.

---

## Execution contract

Follow stages in order. Do not add files beyond this plan. If `ASTRAL_PROD_URL` is missing, prod fetch fails, or candidate id(s) are not confirmed, stop and comment on **AST-438** using the 🛑 blocked template.

---

## Files changed (planned)

| File | Change | Layer |
|------|--------|-------|
| `scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py` | New CLI: read-only prod fetch, local DB read, diff + rubric validation, writes markdown + debug JSON | scripts |
| `docs/features/administrator/ast-438-prompt-diff-local-vs-production.md` | **Deliverable:** prompt/agent diff report (generated by script) | docs |
| `docs/features/administrator/ast-438-rubric-validation.md` | **Deliverable:** per-candidate rubric validation summary (generated by script) | docs |
| `debug/spikes/AST-438/` | Raw JSON snapshots (`prod_agent.json`, `prod_agent_task.json`, `local_*`, `diff_*.json`) — never commit | debug |

No changes to `src/core/agent.py`, `src/core/consult.py`, `src/core/candidate.py`, `src/utils/config.py`, or UI.

---

## Task scope (keys included in prompt diff)

Build a module-level frozenset `AST438_TASK_KEYS` in the spike script (do not add to `config.py`):

**Consult (Phase D — `TASK_CONFIG` phase `"D. Job Analysis"`):**

- `qualify_job_listings`, `evaluate_jd`, `grade_do`, `grade_get`, `grade_like`

**Craft rubrics (Phase B — keys matching `craft_*_rubric` or `craft_company_prefilter`):**

- `craft_company_prefilter`, `craft_joblist_rubric`, `craft_jobdesc_rubric`, `craft_get_rubric`, `craft_do_rubric`, `craft_like_rubric`

**Agent rows:** For each `agent_id` referenced by any included `agent_task` row (`current = 1`), include that `agent` row in the diff.

**Explicitly excluded:** `candidate` table content in diff reports (except designated rows in Stage 4 rubric validation), `dispatch_task`, job/company tables, AST-373/AST-381 automation, `push_tables_to_prod.py` / `upsert_tables_from_prod.py` (no merge).

---

## Stage 0: Preconditions and candidate designation

**Done when:** Linear comment on **AST-438** records prod URL availability and the candidate id list Susan chose; build does not run Stage 4 until ids are listed in that comment or ticket body.

1. Verify `ASTRAL_PROD_URL` is set (env or repo `.env`, same loader as `scripts/sync_from_prod.py`). If unset, 🛑 block — Susan must provide Railway base URL.
2. Post a Linear comment on **AST-438**:

   ```
   AST-438 plan ready — need candidate id(s) for rubric validation (Stage 4).
   Reply with one or more astral_candidate_id values (e.g. somerset).
   Prompt diff (Stage 3) does not require candidates.
   — Ada
   ```

3. **Wait for Susan’s reply** before executing Stage 4. Stages 1–3 may proceed in parallel with waiting only if Susan already named candidates in the ticket; as of plan date, none are named — treat Stage 0 as blocking for Stage 4.

---

## Stage 1: Spike script scaffold

**Done when:** `python3 scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py --help` exits 0 and documents all flags.

1. Create `scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py` with `argparse`:
   - `--out-dir` default `debug/spikes/AST-438` (create parents).
   - `--docs-dir` default `docs/features/administrator`.
   - `--candidates` optional repeatable `astral_candidate_id` (required for rubric stage).
   - `--prompt-only` / `--rubrics-only` flags to run subsets.
   - `--skip-prod` (local-only rubric pass; for offline dev).
2. Reuse prod HTTP pattern from `scripts/sync_from_prod.py`: `GET {ASTRAL_PROD_URL}/api/admin/data/table/{table}` and `?schema_only=1` — **read only**, never POST upsert/import.
3. Local DB path: `ASTRAL_CONFIG["db_dir"] / "astral.db"` via `from src.utils.config import ASTRAL_CONFIG` after `sys.path` insert (same as other scripts).
4. For `agent_task`, filter rows where `task_key in AST438_TASK_KEYS` and `current == 1` (column may be int `1` in SQLite rows).
5. `python3 -m py_compile scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py`.

---

## Stage 2: Snapshot local and production tables

**Done when:** `debug/spikes/AST-438/` contains paired JSON files for `agent` and `agent_task` (filtered), with row counts logged to stdout.

1. Implement `fetch_prod_table(table: str) -> dict` returning `{"columns": [...], "rows": [...]}`.
2. Implement `fetch_local_table(conn, table: str)` with same shape (stdlib `sqlite3`, `row_factory` or list rows like prod API).
3. Write:
   - `prod_agent_task_ast438.json`, `local_agent_task_ast438.json`
   - `prod_agent_ast438.json`, `local_agent_ast438.json` (only agents referenced by filtered tasks)
4. If schema columns differ between prod and local for either table, write `schema_mismatch.json` and 🛑 block (do not silently compare).

---

## Stage 3: Prompt diff markdown

**Done when:** `docs/features/administrator/ast-438-prompt-diff-local-vs-production.md` exists and sections below are populated.

1. Compare each included `task_key` on fields (normalize `None` → `""` before compare):
   - `agent_id`, `user_prompt`, `cache_prompt`, `nocache_prompt`, `system_prompt`, `run_next`
2. Classify each field: `IDENTICAL` | `WHITESPACE_ONLY` (strip equal, raw differ) | `CONTENT_DIFF` | `LOCAL_MISSING` | `PROD_MISSING`.
3. For each `CONTENT_DIFF` on `craft_*_rubric` keys, add a subsection **Craft rubric admin task** summarizing whether diff is likely prompt drift vs intentional prod edit (factual only — no sync recommendation beyond “Susan review”).
4. Compare referenced `agent` rows on: `content`, `model_code`, `temperature`, `max_tokens` (same classification).
5. Markdown structure:

   ```markdown
   # AST-438 — Prompt diff (local vs production)
   Generated: <ISO8601 UTC>
   Prod: <ASTRAL_PROD_URL host only, no secrets>

   ## Executive summary
   - Tasks compared: N
   - Agent tasks with any CONTENT_DIFF: ...
   - Agents with CONTENT_DIFF: ...

   ## Per-task diffs
   ### <task_key>
   ...

   ## Per-agent diffs
   ### <agent_id>
   ...
   ```

6. Add closing **Follow-ups (out of scope):** bullet list referencing AST-373 / AST-381 as optional future sync paths — no implementation here.
7. `git add` only the generated markdown under `docs/features/administrator/`.

⚠️ **Decision:** Use exact string equality for `CONTENT_DIFF`; do not attempt semantic diff or token-level normalization beyond `WHITESPACE_ONLY`.

---

## Stage 4: Rubric validation markdown

**Done when:** `docs/features/administrator/ast-438-rubric-validation.md` exists for each `--candidates` id, and report distinguishes **candidate-data** vs **admin-task** issues.

**Precondition:** Stage 0 candidate ids confirmed.

1. For each candidate id, `database.get_candidate(candidate_id)` (import `src.data.database as database` in script — scripts may import data for read-only diagnostics per existing spike patterns).
2. For each key in `RUBRIC_CRITERIA_ARTIFACT_KEYS` from `src.utils.config` (`joblist_rubric`, `jobdesc_rubric`, `get_rubric`, `do_rubric`, `like_rubric`, plus `company_prefilter`):
   - Load `candidate_data["artifacts"][key]` list.
   - Per criterion index, record pass/fail for:
     - `label` non-empty str
     - `content` non-empty str
     - Trailing grade table: call `src.utils.rubric_text.ensure_criterion_grade_table(item)` in try/except; capture `grade_descriptions` length
     - `importance` int 1–10 (use same bounds as `ASTRAL_CONFIG["consult_importance"]`)
     - `code` present, unique within artifact (WARN if missing; FAIL if duplicate codes)
   - Run `normalize_rubric_artifacts_on_save` on a **deep copy** of artifacts; if `ValueError`, capture message as validation failure (do not write back to DB).
3. **Admin-task cross-check (read-only):** For each `craft_*_rubric` task key, note `TASK_CONFIG[task_key]["response_schema"]` expects `criteria[].label` + `criteria[].content` only — document gap if live artifacts use `code` / `importance` / `grade_descriptions` (expected product extensions per `docs/features/candidate/CANDIDATE_DATA_MODEL.md`).
4. Map each artifact key to consult consumer via `CONSULT_CONFIG` / `rubric_artifact` where applicable (informational table in report).
5. Markdown structure per candidate:

   ```markdown
   # AST-438 — Rubric validation
   Candidate: <id>
   Generated: <ISO8601 UTC>

   ## Summary
   | Artifact key | Criteria | Issues |
   ...

   ## Findings
   ### <artifact_key>
   - Vector "<label>" (code=...): <issue list>

   ## Root-cause hints
   - Candidate-data: ...
   - Admin-task / prompt: ... (cite Stage 3 diff if craft task prompts differ)
   ```

6. `git add` `docs/features/administrator/ast-438-rubric-validation.md`.

---

## Stage 5: Publish deliverables and Linear handoff

**Done when:** Both markdown deliverables are committed on `dev-ada`, cherry-picked to `origin/sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic`, Linear at **Plan Ready** with labels and GitHub link.

1. Run full script once prod + candidates are ready:

   ```bash
   python3 scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py \
     --candidates <id1> [--candidates <id2> ...]
   ```

2. Commit on `dev-ada` (subject must include `AST-438`):

   - `docs(AST-438): diagnostic — prompt diff local vs production`
   - `docs(AST-438): diagnostic — rubric validation`  
   (One commit per stage is fine during **build-astral**; for plan-only pass, only this plan file is committed.)

3. Publish per **build-astral** §6: throwaway worktree on `origin/sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic`, cherry-pick doc commit(s), push, remain on `dev-ada`.

4. Linear comment with paths, self-assessment, link to GitHub blob on the **sub/** ref.

---

## Self-Assessment

**Scope:** `Single-Component` — One spike script plus two generated markdown deliverables; no production code paths.

**Conf:** `Medium` — Prod read API and Susan’s candidate list are external prerequisites; comparison rules are explicit and reuse existing rubric helpers.

**Risk:** `low` — Read-only prod access and local DB reads; mistaken upsert is out of scope and forbidden by script design.

---

## Plan vs ASTRAL_CODE_RULES

- §1.2 / §3.3: Spike script may import `database` for reads; no new core/UI modules; no UI→data imports.
- §2.1: Validation bounds from `ASTRAL_CONFIG["consult_importance"]` and `RUBRIC_CRITERIA_ARTIFACT_KEYS`; task inventory is script-local `AST438_TASK_KEYS`, not a new config block.
- §3.6: Spike output under `debug/spikes/AST-438/`; committed artifacts are markdown in `docs/features/administrator/` only.
- §2.6: No state transitions or dispatch behavior changes.

No conflicts requiring `conf-!!-NONE`.

---

## Review (build-astral)

**Built by Ada.** Branch `sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic`.

| Stage | Status |
|-------|--------|
| 0 | `ASTRAL_PROD_URL` present in `.env` (`astral.up.railway.app`); candidate id(s) still needed from Susan for Stage 4 |
| 1–2 | `scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py` — snapshots to `debug/spikes/AST-438/` |
| 3 | `docs/features/administrator/ast-438-prompt-diff-local-vs-production.md` generated |
| 4 | **Deferred** — no `--candidates` until Susan replies on ticket |
| 5 | Publish + Code Complete |

**Commits:** `c9f9ba1b` (script), `7a4864c9` (prompt diff + build stub)

## Radia review

**Reviewed:** `origin/dev`…`origin/sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic` (no `origin/ftr/AST-438`; AST-434 sub-issue).

### What's solid

| Area | Notes |
|------|--------|
| Plan fidelity | Read-only spike: plan doc, prompt-diff template, `ast438_admin_prompt_rubric_diagnostic.py` (GET + local `data/spikes/ast438/` writes only). |
| Boundaries | No prod writes; no job-list UI (AST-437 sibling); no sync/regeneration. |
| Deliverables | Markdown under `docs/features/administrator/` suitable for Susan review. |

### Issues

| Severity | Item |
|----------|------|
| **fix-now** | 0 |
| **discuss** | 1 — Rubric validation section needs **Susan-designated candidate(s)** before deep pass (per ticket); confirm names in plan comment or run output. |
| **advisory** | 0 |

### Recommended actions

| Action | Owner |
|--------|--------|
| Cherry-pick doc commit onto `dev-ada` | Ada |
| Fill rubric-diff markdown after candidate id confirmed | Ada / Susan |

## Resolution (resolve-astral) — 2026-05-18

**Radia:** fix-now **0** — no product code changes.

**Discuss (candidate ids):** Stage 4 rubric validation (`ast-438-rubric-validation.md`) remains **deferred** until Susan names `astral_candidate_id`(s) on **AST-438**. Prompt diff deliverable is complete; re-run:

`python3 scripts/spikes/ast438_admin_prompt_rubric_diagnostic.py --candidates <id>`

**Doc:** Merged Radia review section (`cf09e974`) into this plan on `dev-ada`.

**Branch for prep-uat:** `sub/AST-434/AST-438-production-readiness-admin-prompt-and-rubric-diagnostic` (parent **AST-434**).
